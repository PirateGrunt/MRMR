#' Pivot a triangle
#' 
#' Triangle evaluation dates are established by adding development lags to the starting point of the origin periods.
#' @export PivotTriangle
#' @param ColumnCategory
#' @param RowCategory
#' @param Value
#' @param data
#' @return Dataframe of pivoted data

PivotTriangle = function(ColumnCategory, RowCategory, Value, data)
{
  arguments <- as.list(match.call())
#   = eval(arguments$OriginPeriods, TriangleData)
}

